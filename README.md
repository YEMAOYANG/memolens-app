# MemoLens Flutter App

AI 驱动的相机记忆工具 - 拍一下，记住一切

## 快速开始

```bash
flutter pub get
flutter run --dart-define=API_BASE_URL=https://api.memolens.app
```

## 项目结构

- `lib/features/` — 按功能模块分层
- `lib/shared/` — 共用组件/API/服务
- `lib/app/` — 路由 + 主题

## 环境变量

复制 `.env.example` 为 `.env` 并填写。
